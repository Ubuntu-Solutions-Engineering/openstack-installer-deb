#!/bin/sh
set -e
if [ ! -d /var/log/openstack ]; then
   mkdir -p /var/log/openstack
fi

ln -sf /usr/share/openstack/uoi-rsyslog.conf /etc/rsyslog.d/99-uoi.conf

service rsyslog restart || true

#DEBHELPER#